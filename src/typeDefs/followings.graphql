scalar DateTime


type Query {
    followings(page: Int, perPage: Int, userId: ID, createdAt: String): [Following]
}

type Mutation {
    addFollowing(input: FollowingInput): [Following]
    # removeFollowing(input: FollowingInput): [Following]
}

type Following {
    following_id: ID
    user_id: ID
    created_at: DateTime
}

input FollowingInput {
    following_id: ID
    user_id: ID
}
